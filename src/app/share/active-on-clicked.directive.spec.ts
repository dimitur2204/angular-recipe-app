import { ActiveOnClickedDirective } from './active-on-clicked.directive';

describe('ActiveOnClickedDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveOnClickedDirective();
    expect(directive).toBeTruthy();
  });
});
